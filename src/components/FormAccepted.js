import React from "react";

const FormAccepted = (props) => {
    return (
        <div className="text-center">
            <h2>{props.text}</h2>
        </div>
    );
};

export default FormAccepted;
